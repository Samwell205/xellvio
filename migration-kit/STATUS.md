# Migration status

## Done on your Supabase project

- **Extensions, schemas, message queues** — `00-prelude.sql` applied.
- **Full schema** — all 175 migrations replayed: 62 tables, 11 enums, 45
  functions, 167 RLS policies, 39 triggers. Matches the live database.
  The two email-queue functions that don't live in migrations were recreated.
- **All data** — every public table copied row-for-row, ids and timestamps
  preserved verbatim (~700k rows).
- **All 62 users** — recreated in `auth.users` with their **original ids**, so
  every account, role, workspace membership, campaign and contact still
  resolves. Verified: 0 orphaned accounts, 0 orphaned roles, 1 admin.
  Passwords cannot be exported from Lovable Cloud, so each user sets a new
  password on first sign-in (no reset emails sent yet — `SEND_RESET=false`).
- **Storage** — all 47 files copied with identical paths (21 `opt-in-assets`,
  26 `campaign-media`; `payment-proofs` and `academy-covers` were empty), so
  every stored URL keeps working.
- **Money reconciled** — total credit balance, seller balances, the full
  credit ledger and message costs match the live database.

Nothing on the live site changed. It is still running on the current database,
untouched, and every tenant account is unaffected.

## Remaining

1. **Final delta sync at cutover.** The live site keeps writing while we work,
   so the fast-moving tables drift by whatever traffic happened since the last
   copy. With sending paused, re-copy:
   `accounts`, `messages`, `message_send_attempts`, `credit_transactions`,
   `events`, `sms_thread_messages`, `link_clicks`, `payments`, `suppressions`,
   `campaign_test_sends`, `campaign_tos_acceptances`, `content_screening_log`.

2. **Point the app at your project.** The deployed app's database credentials
   are generated by Lovable Cloud and cannot be repointed from inside this
   project. Running the app against your own Supabase requires connecting it
   through the Supabase integration; the migrated data is already in place and
   waiting.

3. **Re-add runtime secrets and auth providers** on the new project, and
   re-point the external webhooks — see Phase 5 and Phase 6 in `README.md`.

## Rollback

Nothing done so far is destructive. The current database is live and complete;
if anything fails, the old system simply keeps running.
